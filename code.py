import random

# База даних творів
library = [
    {"назва": "Кайдашева сім’я", "автор": "Іван Нечуй-Левицький", "рік": 1879, "жанр": "роман", "читань": 0},
    {"назва": "Тіні забутих предків", "автор": "Михайло Коцюбинський", "рік": 1911, "жанр": "повість", "читань": 0},
    {"назва": "Заповіт", "автор": "Тарас Шевченко", "рік": 1845, "жанр": "вірш", "читань": 0},
    {"назва": "Маруся", "автор": "Григорій Квітка-Основ’яненко", "рік": 1834, "жанр": "повість", "читань": 0},
]

# Генератор нових надходжень
def generate_new_books():
    нові = [
        {"назва": "Сон", "автор": "Тарас Шевченко", "рік": 1844, "жанр": "вірш", "читань": 0},
        {"назва": "Лісова пісня", "автор": "Леся Українка", "рік": 1911, "жанр": "драма-феєрія", "читань": 0},
    ]
    for книга in нові:
        yield книга

# Функція пошуку
def search_books(критерій, значення):
    результати = []
    for книга in library:
        if str(книга[критерій]).lower() == str(значення).lower():
            результати.append(книга)
    return результати

# Функція перегляду статистики читань
def reading_statistics():
    print("\n📊 Статистика читань:")
    for книга in library:
        print(f"- {книга['назва']} ({книга['автор']}): {книга['читань']} читань")

# Основне меню
def main():
    # Додаємо нові надходження
    for нова_книга in generate_new_books():
        library.append(нова_книга)

    while True:
        print("\n📚 Електронна бібліотека української літератури")
        print("1. Пошук за автором")
        print("2. Пошук за роком")
        print("3. Пошук за жанром")
        print("4. Прочитати випадкову книгу (збільшити статистику)")
        print("5. Переглянути статистику читань")
        print("0. Вийти")
        
        вибір = input("Оберіть опцію: ")

        if вибір == "1":
            автор = input("Введіть ім’я автора: ")
            results = search_books("автор", автор)
        elif вибір == "2":
            рік = input("Введіть рік: ")
            results = search_books("рік", рік)
        elif вибір == "3":
            жанр = input("Введіть жанр: ")
            results = search_books("жанр", жанр)
        elif вибір == "4":
            книга = random.choice(library)
            книга["читань"] += 1
            print(f"\n📖 Ви прочитали: {книга['назва']} — {книга['автор']}")
            continue
        elif вибір == "5":
            reading_statistics()
            continue
        elif вибір == "0":
            print("Дякуємо за використання електронної бібліотеки!")
            break
        else:
            print("❗ Невірна опція.")
            continue

        # Виведення результатів пошуку
        if results:
            print("\n🔎 Знайдені твори:")
            for к in results:
                print(f"- {к['назва']} ({к['автор']}, {к['рік']}), жанр: {к['жанр']}")
        else:
            print("❌ Нічого не знайдено.")

# Запуск програми
main()
